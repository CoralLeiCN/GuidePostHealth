# Project instructions

## Deployment

- Treat this repository as local-only by default.
- Do not publish or deploy it to ChatGPT Sites or any other hosting service unless the user explicitly requests deployment in the current conversation.
- Local preview and production-build checks are allowed, but the presence of `.openai/hosting.json` is not authorization to publish.
